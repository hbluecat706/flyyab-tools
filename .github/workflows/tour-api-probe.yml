name: FlyYab Tour API Probe
on:
  push:
    branches: [tour-api-probe-20260907]
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Extract live route hydration
        run: python tools/tour_hydration_probe.py
