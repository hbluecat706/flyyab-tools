name: FlyYab Tour API Probe
on:
  push:
    branches: [tour-api-probe-20260907]
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Direct server-side FlyYab tour API
        run: python tools/tour_direct_probe.py
